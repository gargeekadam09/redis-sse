services:
  - type: web
    name: redis-sse-backend
    env: node
    buildCommand: cd chat-app/backend && npm install
    startCommand: cd chat-app/backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000