{
  "name": "redis-sse-chat",
  "version": "1.0.0",
  "scripts": {
    "build": "cd chat-app/backend && npm install",
    "start": "cd chat-app/backend && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}